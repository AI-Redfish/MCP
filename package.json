{
  "name": "ai-redfish-mcp-collection",
  "version": "0.1.0",
  "description": "MCP 服务器集合：仓库根目录作为启动器（Launcher），根据参数动态加载 mcps/ 目录下的各个 MCP 子服务（每个子服务为 core + cli + mcp 三包工作区）",
  "license": "MIT",
  "bin": {
    "mcp-collection": "bin/mcp-launcher.js"
  },
  "scripts": {
    "start": "node bin/mcp-launcher.js",
    "list": "node bin/mcp-launcher.js list",
    "build": "node bin/mcp-launcher.js build"
  },
  "engines": {
    "node": ">=18"
  }
}
