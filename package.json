{
  "name": "ai-redfish-mcp-collection",
  "version": "0.1.0",
  "description": "MCP 服务器集合：仓库根目录作为启动器（Launcher），根据参数动态加载 mcps/ 目录下的各个 MCP 子服务",
  "license": "MIT",
  "bin": {
    "mcp-collection": "bin/mcp-launcher.js"
  },
  "scripts": {
    "start": "node bin/mcp-launcher.js",
    "list": "node bin/mcp-launcher.js list"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "engines": {
    "node": ">=18"
  }
}
